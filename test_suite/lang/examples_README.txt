For each example.zig file, run on the command line using the dotnet run function.

EX. If running example-1.zig, type in:
dotnet run example-1.zig

then open the file manually or call it from the command line:
output.svg
